---
const response = await fetch("http://localhost:3000/enlaces");
const DATA = await response.json();
---

<div class="w-full  py-6 md:p-6 text-center sm:p-8">
  <div>
    <div>
      <h1 class="font-bold text-4xl text-gray-50">Enlaces De Interés</h1>
      <p class="font-light text-lg pt-3
      ">
        Descubre las herramientas digitales que mantienen nuestra comunidad
        educativa en sintonía. Ingresa a los siguientes enlaces de interés de
        nuestra institución educativa
      </p>
    </div>
  </div>
  <div
    class="justify-center bg-slate-400  grid grid-cols-2 gap-1 md:grid-cols-4 md:gap-4 p-4 rounded-lg mt-4"
  >
    {
      DATA.map((enlace: any) => (
        <a
          href={enlace.href}
          target="_blank"
          class="w-full sm:w-auto hover:bg-gray-700 focus:ring-4 focus:outline-none focus:ring-gray-300 text-white rounded-lg inline-flex items-center justify-center px-4 py-2.5 dark:bg-gray-700 dark:hover:bg-gray-600 dark:focus:ring-gray-700"
        >
          <img src={enlace.img} alt="logo " />
        </a>
      ))
    }
  </div>
</div>

<style>
  text {
    width: 100%;
    height: auto;
  }
